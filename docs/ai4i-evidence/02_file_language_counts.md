# Repository File Type Summary

- Scan scope: workspace root and subfolders.
- Total files matched by glob: ~238 (various extensions).

## Top languages / file types

- **TypeScript**: 163 files (combined `.ts` = 83, `.tsx` = 80)
- **JavaScript**: 27 files (`.js`)
- **Markdown**: 10 files (`.md`)
- **JSON**: 9 files (`.json`)
- **YAML / workflow (`.yml`)**: 4 files
- **JPEG images**: 4 files (`.jpg`)
- **PNG images**: 3 files (`.png`)
- **CSV**: 3 files (`.csv`)
- **DOCX**: 3 files (`.docx`)
- **Others**: small counts for `.py`, `.html`, `.css`, package-locks, `.env` examples

## Notes
- Primary development language: TypeScript (frontend + backend services). This repository contains a monorepo-like layout with a TypeScript client (`client/`) and TypeScript server (`server/`).
- Some older JS files exist under the top-level `src/` and `tests/` (legacy pieces).
- Binary assets (images, docx) are present in `client/public/` and repo root.

If you want, I can create a detailed CSV with counts per extension and a pie chart diagram (Mermaid).