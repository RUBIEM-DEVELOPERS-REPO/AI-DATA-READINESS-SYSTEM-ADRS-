I already have a working document intelligence system. Do NOT redesign it from scratch and do NOT replace its core architecture. Your job is to improve, harden, validate, and extend the existing system so it becomes production-grade.

Existing system architecture that must be preserved:
- raw documents are ingested
- an LLM analyzes the inputs
- extracted content is mapped into a Canonical Data Model (CDM)
- from the CDM, golden records and 3 dataset types are created:
  1. RAG chunk dataset
  2. Knowledge graph dataset
  3. ML feature dataset

Important constraints:
- the system is intentionally industry-agnostic and department-agnostic at the core
- do not make it HR-only, finance-only, or legal-only
- do not hardcode department-specific behavior into the core architecture
- instead, improve the system by introducing configurable profiles, validators, schema extensions, and routing policies that allow specialization without losing the generic platform design
- preserve the existing architecture and improve it incrementally
- assume substantial development has already been done, so focus on enhancement, not replacement

Observed issues from current outputs that must be addressed:
- 8 CV documents were ingested but only 5 DOCUMENT nodes appear in the knowledge graph, so document coverage, lineage, or CDM mapping is incomplete
- graph edges are too shallow and currently over-rely on MENTIONED_IN instead of richer semantic relationships
- entity typing is incorrect in multiple cases, for example:
  - ZENT ZESA classified as PERSON
  - NESARI Project classified as PERSON
  - Fredda Rebecca Gold Mine classified as PERSON
- some document display names are unstable or low quality, for example:
  - “Available upon request”
  - “Class 1 Driver”
  - autogenerated IDs like “CV-fe532cc2”
- document properties are inconsistently represented:
  - some are proper typed fields
  - some are flattened ad hoc fields
  - some are JSON strings embedded inside fields
- normalization of phone numbers, dates, names, and identifiers is inconsistent
- golden record generation appears incomplete, weakly validated, or under-triggered
- ML feature exports are too thin for realistic downstream analytics or modeling
- the current output suggests the CDM may be too permissive and may be accepting low-quality mappings

Your objective:
Improve the current system so it becomes a robust, production-grade, domain-agnostic document intelligence platform that can:
- recognize many document types
- route documents through configurable extraction strategies
- map outputs into a strict CDM
- generate reliable golden records
- export high-quality datasets for RAG, KG, and ML use cases
- remain extensible for any department or industry without changing the core design

Key principle:
The system core remains generic. Specialization must happen through profiles, schemas, ontologies, validation packs, and extraction policies, not by rewriting the platform.

Please improve the current system along the following dimensions:

1. Core Architecture Hardening
Do not replace the pipeline. Keep:
raw document -> extraction -> CDM mapping -> golden records -> dataset exports

Improve it by adding:
- stronger validation layers
- normalization layers
- profile-driven extraction behavior
- better provenance
- stronger error handling
- quality gates
- observability and auditability

2. Profile-Driven Document Understanding
Introduce a pluggable profile system instead of hardcoding departments or industries.

Design profiles such that:
- core system remains generic
- profiles can define:
  - document families
  - extraction targets
  - entity types
  - relationship types
  - normalization rules
  - validation rules
  - export relevance
- examples of profiles may include HR, Finance, Legal, Procurement, Operations, but these should be examples only, not baked into the core logic
- allow fallback to generic extraction when no profile confidently matches

3. Document Classification and Routing
Improve current document understanding so the system can:
- classify document family
- optionally infer likely department or business context
- assign confidence scores
- choose an extraction profile
- preserve top-k candidate classifications for review

Make this configurable and profile-driven, not rigid.

4. Canonical Data Model Hardening
Audit and improve the existing CDM without changing its purpose.

The CDM must remain the canonical backbone of the system, but improve it so that it becomes:
- strict
- typed
- versioned
- auditable
- provenance-aware
- normalization-aware
- safe for production

Ensure the CDM supports at minimum:
- Document
- Evidence
- Entity
- Relationship
- CanonicalRecord
- GoldenRecord
- DatasetExportRecord

Requirements:
- no JSON strings embedded inside scalar fields
- use typed nested objects instead of loose blobs
- enforce enums where appropriate
- support schema versioning and migrations
- every mapped item must carry provenance and confidence
- invalid mappings must be rejected, quarantined, or flagged instead of silently accepted

5. Entity Typing and Normalization
Improve post-processing to reduce misclassification and improve canonicalization.

The system should be able to distinguish entity types such as:
- PERSON
- ORGANIZATION
- PROJECT
- ROLE
- SKILL
- CERTIFICATION
- LOCATION
- LANGUAGE
- DOCUMENT
- and allow profile-defined extensions

Add normalization utilities for:
- names
- organizations
- phone numbers
- emails
- dates and date ranges
- job titles
- qualifications
- identifiers
- profile-specific vocabularies

6. Relationship Extraction Improvement
Do not remove the current graph generation. Improve it so it produces useful semantic relationships beyond MENTIONED_IN.

Requirements:
- preserve ENTITY -> DOCUMENT provenance links
- add support for configurable semantic relationships
- relationship types must come from profile packs plus a generic fallback ontology
- every relationship must include evidence references and confidence

7. Golden Record Improvement
Do not replace the concept of golden records. Improve how they are created from the CDM.

Add:
- better entity resolution
- conflict handling
- evidence ranking
- confidence aggregation
- canonical identity selection
- duplicate detection
- explanation trail for each golden record

Golden records must be explainable and reproducible from the source evidence.

8. Dataset Export Improvement
Improve all three export builders while keeping them as separate outputs from the same CDM backbone.

A. RAG export improvements
- better chunk boundaries
- provenance-preserving chunking
- PII flags and redaction state
- chunk trust score
- linked entities
- export quality score
- support for layout-aware chunking when available

B. Knowledge graph export improvements
- typed nodes
- typed edges
- semantic relationships
- provenance
- confidence
- canonical IDs
- graph completeness checks

C. ML feature export improvements
Add richer and more realistic features such as:
- entity document frequency
- source evidence count
- confidence aggregates
- normalization quality score
- entity type certainty
- relationship count
- document diversity
- profile consistency
- completeness score
- golden record match score
- ambiguity score
- provenance quality score

9. Quality Gates and Validation
Add production-grade quality checks across the current system.

Implement validators for:
- document coverage
- schema conformance
- normalization completeness
- entity typing quality
- relationship completeness
- golden record eligibility
- export readiness
- provenance completeness

Add explicit error classes:
- extraction_error
- mapping_error
- validation_error
- normalization_error
- resolution_error
- export_error

10. Evaluation Framework
Add a measurable evaluation framework without changing the system’s core architecture.

Track:
- document classification accuracy
- field extraction precision and recall
- entity typing accuracy
- relationship extraction quality
- CDM conformance rate
- golden record quality
- export completeness
- per-profile performance
- fallback generic-mode performance

11. Human-in-the-loop
Add review workflows for:
- low-confidence classification
- ambiguous entity typing
- invalid CDM mappings
- golden record conflicts
- weak relationship extraction
- low-quality exports

12. Replit-Friendly Incremental Refactor
I am coding in Replit, and the codebase already exists.

Do NOT scaffold a totally new application unless necessary.
Instead:
- propose an incremental refactor plan
- preserve existing modules where possible
- identify what to add, what to wrap, what to validate, and what to refactor carefully
- generate code that can be integrated into an existing project
- prefer Python
- use pydantic for schemas and validators
- add logging, retries, audit trails, and testability
- design for future async scaling, but keep current implementation simple

What I want from you:
1. Audit the likely weak points in my existing system based on the issues listed above
2. Propose improvements that preserve the existing architecture
3. Define a profile/plugin mechanism for domain-specific or department-specific behavior without making the platform domain-bound
4. Strengthen the Canonical Data Model
5. Improve golden record logic
6. Improve all 3 dataset export builders
7. Add validation, normalization, and quality gates
8. Suggest incremental code changes suitable for an existing Replit codebase
9. Provide pydantic schema examples and code skeletons
10. Give a staged implementation roadmap that starts with the highest-value low-risk improvements

Important implementation rule:
Whenever you suggest a change, first prefer:
- wrapping existing logic
- validating existing outputs
- normalizing existing records
- adding profile-driven extensions
- introducing stricter contracts around existing behavior

Only suggest deeper refactors if they are necessary and justified.

Please deliver:
1. a production-hardening plan for the current system
2. a likely root-cause analysis of the current output problems
3. an improved CDM design that preserves the current architecture
4. a profile/plugin architecture for specialization without losing domain agnosticism
5. example pydantic models
6. validation and normalization rules
7. improved relationship extraction design
8. improved golden record logic
9. improved RAG/KG/ML export logic
10. an incremental implementation roadmap for an existing codebase
11. examples of test cases and evaluation metrics