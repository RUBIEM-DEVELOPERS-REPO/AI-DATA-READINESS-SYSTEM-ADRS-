// Phusion Passenger (cPanel Application Manager) entry point.
// Passenger automatically runs this file when the app starts.
// It simply loads the pre-built production bundle.
require("./dist/index.cjs");
