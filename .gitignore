node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.local/
uploads/
scratch/
scratch.ts
migrations/

# Vercel — project metadata (created by `vercel link`; contains project IDs)
# Commit .vercel/project.json if you want CI to auto-detect the project
# without VERCEL_ORG_ID/VERCEL_PROJECT_ID secrets (not recommended for private repos)
.vercel

# Local Vercel env overrides (use .env.vercel.example as the reference)
.env.vercel